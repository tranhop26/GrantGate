{
  "name": "@grantgate/contracts",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "python -m pytest tests -q && node --test scripts/*.test.mjs",
    "lint": "uvx --from genvm-linter genvm-lint check grantgate.py"
  },
  "dependencies": {
    "genlayer-js": "^1.1.8"
  }
}
